[project]
name = "backend"
version = "0.1.0"
description = "backed for `khalto`"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "blinker>=1.9.0",
    "flask>=3.1.3",
    "scipy>=1.17.1",
]
